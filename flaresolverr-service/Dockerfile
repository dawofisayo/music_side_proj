FROM ghcr.io/flaresolverr/flaresolverr:latest

# FlareSolverr runs on port 8191 by default
EXPOSE 8191

# Set environment variables for better stability
ENV LOG_LEVEL=info
ENV LOG_HTML=false
ENV TZ=UTC

# The image already has the entrypoint configured
# No need to override CMD or ENTRYPOINT

